# Frontend Environment Variables
# Copy to .env.local for development or .env.production for production

# API Base URL
# Development: Uses /api proxy (see vite.config.js)
# Production: Points to Render backend
VITE_API_URL=/api
